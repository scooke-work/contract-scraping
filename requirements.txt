# Core scraper runtime (scrape_contracts.py).
requests>=2.31
PyYAML>=6.0
trafilatura>=2.0
beautifulsoup4>=4.12

# Decision B: parse PDF / Word EDGAR exhibits to TEXT in-memory (the source file
# is never stored). parse_binaries defaults on; without these two, such exhibits
# are simply skipped (not a fatal error).
pdfminer.six>=20221105
python-docx>=1.1
